#include <stdio.h>
int main(void)
int calculate_change(coins_total,price)
{
int menu_number;
float coins_total,coins_E=3.5,coins_C=4.5,coins_M=5.5;
printf("enter a number from the list: \n 1.Espresso (%f) \n 2.Cappuccino (%f) \n 3.Mocha (%f)",coins_E,coins_C,coins_M);
scanf("%i",&menu_number);

printf("Insert coins.Use coins of 1 and 0.5 only");
scanf("%f",&coins_total);
}